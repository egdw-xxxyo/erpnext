# Панель адміністрування: сторінки навігації та локальний режим без SSH

- **Ліва навігація** — панель тепер розбита на 5 сторінок замість однієї великої:
  Dashboard, Backups, Deploy, Configuration, Information. Консоль завдання
  спільна для всіх сторінок.
- **Локальний режим без SSH (`OPS_LOCAL_MODE=1`)** — для машини без запущеного
  sshd (ноутбук розробника). Вхід перевіряє лише `OPS_ALLOWED_USERS`, без
  пароля; команди виконуються прямо в контейнері ops (docker-сокет і
  репозиторій монтуються з хоста через `docker-compose.ops.local.yml`).
  Ніколи не вмикати на спільному чи продовому хості. Деталі:
  `docs/ops-dashboard.md` → «Локальний режим без SSH».
- **Кнопка «Test connection» на панелі FTP backup target** — перевіряє
  з'єднання з поточними значеннями форми (без обов'язкового збереження
  спочатку), показує вміст `remote_dir` як доказ успіху.
- **Виправлено протокол позахостових копій: FTP, а не SFTP.** Ціль передачі
  виявилась звичайним FTP з опортуністичним TLS на порту 21 (підтверджено
  реальним акаунтом Tucha) — попередня реалізація ходила по SFTP/SSH на
  порт 22, який на цьому хості просто не слухає. Жодна реальна передача
  відколи існує ця функція не могла спрацювати; перевірялося лише
  збереження налаштувань. Перейменовано `sftp_config.py`/`sftp.py` →
  `ftp_config.py`/`ftp.py`, `/settings/sftp` → `/settings/ftp` — назви
  тепер відповідають реальності.
- **Виправлено TLS-довіру FTPS-цілі**: сертифікат Tucha не перевіряється
  системним ланцюжком довіри curl (curl 60) — додано `-k` до всіх curl-звернень
  до FTP-цілі.
- **Сторінка Backups перероблена**: інформаційна картка (час останньої
  локальної копії, розклад наступної), кнопка «Create local backup», кнопка
  «Clean old backups» (лишає тільки найновішу копію, з підтвердженням), меню
  з трьох крапок на кожному рядку (Push/Restore/Remove — кожна дія з
  підтвердженням). Секція «Remote backups» тепер показує хост і теку цілі.
  Версія релізу тепер відображається біля IP хоста в шапці на всіх сторінках.
- **Виправлено повільний перехід на /backups**: сторінка більше не чекає
  повний SSH-збір статистики перед рендером — локальні й віддалені бекапи
  вантажаться асинхронно зі своїм станом «завантаження», як усюди інде.
- **Показано статус синхронізації** на локальних копіях: пілюля «synced»,
  якщо копія вже є на віддаленій цілі, «local only» — якщо ні.
- **Багато FTP-цілей замість однієї**: на сторінці Configuration тепер
  таблиця цілей (Add/Edit/Remove), кожна з назвою і позначкою призначення
  (prod/dev/test) — наприклад, dev-інстанс може тримати ціль prod, щоб
  завантажити прод-копію для локального тестування. У розкладі («Push to»)
  тепер обирається конкретна ціль. Сторінка Backups показує всі налаштовані
  цілі окремими картками замість однієї. Стара єдина ціль (`ftp_config.enc`)
  автоматично мігрує в нову таблицю при першому зверненні.
- **`./deploy ops rebuild`** (кнопка «Rebuild dashboard» на Deploy) більше не
  падає з «OPS_SESSION_SECRET is missing a value» — `.ops.env` тепер
  підвантажується самою `ops_dc`, а не лише при старті.
- **Меню з трьох крапок закривається** по кліку поза ним або по вибору дії
  (раніше залишалось відкритим).
- **Новий пункт меню «Space management»**: панель Disk (перенесена з
  Dashboard) з розбивкою Docker-образів/кешу, зведення по локальних бекапах
  (місце, кнопка «Clean old backups») і кнопка **«Start safe clean»** —
  прибирає dangling Docker-образи, невикористаний build-кеш і вакуумить
  systemd journal до 100 МБ. Ніколи не чіпає бекапи, базу сайту чи файли
  всередині `sites/`.
- **Виправлено «Update repo»**: якщо `origin` на хості налаштований по
  `https://` без збережених креденшлів, `git pull` падав з «could not read
  Username for 'https://github.com'». Тепер на панелі Configuration кожен
  користувач ops може додати власний SSH deploy-key (зберігається
  зашифровано, як пароль FTP-цілі) — «Update repo» і «Switch branch»
  підставляють його через `GIT_SSH_COMMAND` тільки на час команди. Працює
  лише якщо `origin` — SSH-адреса (`git@github.com:...`); якщо він досі
  `https://`, змінити вручну на хості: `git remote set-url origin
  git@github.com:egdw-xxxyo/erpnext.git`.
- **Знайдено і виправлено справжню причину** того самого симптому на dev:
  `origin` уже був SSH, але сабмодуль `frappe` у `.gitmodules` досі був
  `https://`. GitHub повертає 401 на анонімний `git-upload-pack` (сама
  видача pack-даних, а не просто перевірка сторінки репозиторію) з IP цього
  хоста навіть для публічних репозиторіїв — тому `curl -I` і відкриття
  сторінки в браузері працювали, а `git fetch`/`./updateRepo` падали з
  «could not read Username». `.gitmodules` тепер вказує на
  `git@github.com:egdw-xxxyo/frappe.git`; `git submodule sync` (крок у
  `./updateRepo`) підхопить це на хості автоматично після `git pull`.
- **FTP-ціль спрощено до Назва + Шлях**: сервер (host/port/user/пароль)
  тепер налаштовується один раз окремо («Configure server…»), кожна ціль —
  лише назва, позначка prod/dev/test і віддалений шлях. Стара ціль з
  власними креденшлами мігрує автоматично при першому відкритті сторінки.
  Додавання/редагування цілі й сервера тепер відкривається у справжньому
  модальному вікні (`<dialog>` з затемненим фоном) замість розгортання
  форми просто на сторінці.
- **Нова кнопка «Hard clean» на Space management**: «Safe clean» видаляє
  лише "dangling" Docker-образи — здебільшого набагато менше числа у
  колонці «Reclaimable» (те число — все, що не використовується жодним
  контейнером, включно зі старим тегованим образом попереднього релізу).
  «Hard clean» (`./deploy space-hard-clean`, з підтвердженням, вводом назви
  сайту) звільняє реально все це число: `docker image prune -a -f` +
  `docker builder prune -a -f`, ціною втрати можливості відкату на
  попередній образ і повільнішої наступної збірки (кеш збірки теж повністю
  чиститься). Пояснення різниці додано під таблицею Docker на сторінці Disk.

**Табель переїхав у зарплатний модуль — на екрані нічого не змінилося.**

Табель відвідуваності, місячний звіт і погодження місяця жили в чужому застосунку (HR), хоч
писалися під наш розрахунок зарплати. Через це кожне оновлення HR доводилося протягувати
крізь наш код, і кожне таке протягування було ризиком зачепити табель.

Тепер весь табель лежить у зарплатному модулі поруч із розрахунком, який його читає.

- сторінка «Табель відвідуваності» відкривається там само і виглядає так само;
- звіт «Місячна таблиця відвідування» лишився під тим самим іменем і в тих самих місцях —
  і в пошуку, і на сторінці HR;
- погодження місяця, позначки днів, години переробітку й недоробітку, експорт в Excel —
  без змін;
- зарплатна відомість і аванс читають табель так само, календар відвідуваності показує ті
  самі позначки.

Заповнені табелі, погодження й нараховані зарплати лишаються як були: нічого не
перераховується й нічого не треба відмічати заново.

- **Виправлено `./deploy build`, що зависав на «Username for
  'https://github.com':»** на dev — та сама блокування GitHub анонімного
  `git-upload-pack` з цього IP, тепер ще й на кроці клонування зовнішніх
  застосунків з `apps.json` (hrms, crm, frappe_whatsapp) і на обчисленні
  `apps-cache-bust` (`git ls-remote`). SSH deploy-key тут не підходить —
  `frappe/crm` нам не належить, ключ туди не додати. Замість цього:
  опціональний GitHub PAT у `~/.github_token` (`GITHUB_TOKEN_FILE`), який
  `./deploy` підставляє через `git -c http.extraheader=...` для `ls-remote`
  на хості й через BuildKit `--secret` (не потрапляє у шар образу) для
  `git clone` усередині `Dockerfile.full`. Без токена — поведінка як
  раніше, анонімно. Також `GIT_TERMINAL_PROMPT=0` глобально в `./deploy`,
  щоб жоден git-виклик більше не зависав на запиті логіна.
