# Copy to .tunnel.env (gitignored) and paste the tunnel token from
# Cloudflare Zero Trust > Networks > Tunnels > your tunnel.
# When present, ./deploy starts the cloudflared sidecar automatically.
TUNNEL_TOKEN=
